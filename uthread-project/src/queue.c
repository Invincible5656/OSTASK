#include "uthread.h"

#include <stddef.h>

/* ── Queue operations (intrusive singly-linked list via uthread_t.next) ──── */

/* TODO Stage 2: enqueue(uthread_t **head, uthread_t **tail, uthread_t *t) */
/* TODO Stage 2: dequeue(uthread_t **head, uthread_t **tail) -> uthread_t * */
/* TODO Stage 2: queue_find(uthread_t *head, int tid)        -> uthread_t * */
/* TODO Stage 2: queue_remove(uthread_t **head, uthread_t **tail, int tid)  */
